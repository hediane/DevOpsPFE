version: '3.7'
services:
 backend:
  build:
   context : ./DevOpsProject/DevOpsProject
   dockerfile : Dockerfile
  container_name : Back
  volumes:
      - ./:/var/www
  expose:
   - "5000"
  depends_on:
   - nginx_proxy
  restart : always

 nginx_proxy:
  build:
    context : ./nginx/back
    dockerfile : Dockerfile
  container_name: nginx_Back
  volumes:
   - ./:/var/www
  ports:
   - "4001:80"
  restart : always
